# Durable Object Migrations

These live inside `src/` because they are bundled into code and applied at runtime.

Each subfolder, e.g. `ChatRoomDO`, should correspond to the classname of a durable object.
