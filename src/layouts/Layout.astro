---
import Logo from "#/assets/logo.svg";
import "#/styles/global.css";
import { NavBarAccount } from "@/components/NavBarAccount";
import HTML from "./HTML.astro";

type Props = {
  class?: string;
  title: string | null;
};

const { title, class: className } = Astro.props;

const serverUser = Astro.locals.user;
const initialUser = serverUser
  ? {
      name: serverUser.name ?? null,
      email: serverUser.email,
      image: serverUser.image ?? null,
    }
  : null;
---

<HTML title={title}>
  <header
    class={`navbar bg-base-300 dark:bg-base-900 p-3 text-stone-600
      dark:text-stone-100`}
  >
    <div class="navbar-start">
      <a href="/" class="link link-primary link-hover text-xl font-medium"
        ><Logo
          aria-label="Chat with Dice logo"
          aria-description="A d6 showing sixes on all sides, with a tail like a speech bubble"
          fill={undefined}
          stroke={undefined}
          class={`h-10 w-10
            [&_.dark-part]:fill-[oklch(from_var(--color-neutral)_0.9_c_h)]
            [&_.die-pips]:fill-[oklch(from_var(--color-primary)_0.6_c_h)]
            [&_.light-part]:fill-[oklch(from_var(--color-neutral)_1_c_h)]
            [&_.stroke]:fill-none
            [&_.stroke]:stroke-[oklch(from_var(--color-neutral)_0.7_c_h)]
            [&_.stroke]:stroke-8`}
        /></a
      >
    </div>
    <div class="navbar-center">
      <h1 class="font-bold lg:text-3xl">
        {title ?? "Chat with Dice"}
      </h1>
    </div>
    <div class="navbar-end gap-4">
      <NavBarAccount client:only initialUser={initialUser} />
    </div>
    <hr />
  </header>
  <main class={`flex-1 w-full ${className} overflow-auto bg-base-200`}>
    <slot />
  </main>
</HTML>
